{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "sticky-banner",
  "title": "Sticky Banner",
  "description": "Banner fixo no topo (sticky) que oculta/mostra conforme a posição de scroll, com botão de fechar interno. Wrapper bg-transparent — cor fica nos children.",
  "dependencies": [
    "motion"
  ],
  "files": [
    {
      "path": "src/components/ui/sticky-banner.tsx",
      "content": "import * as React from \"react\"\nimport { motion, useMotionValueEvent, useScroll } from \"motion/react\"\nimport { cn } from \"@/lib/utils\"\nimport { type StickyBannerProps } from \"./sticky-banner-types\"\n\nfunction StickyBanner({\n  className,\n  children,\n  hideOnScroll = false,\n  ...props\n}: StickyBannerProps) {\n  const [open, setOpen] = React.useState(true)\n  const { scrollY } = useScroll()\n\n  useMotionValueEvent(scrollY, \"change\", (latest) => {\n    if (hideOnScroll && latest > 40) {\n      setOpen(false)\n    } else {\n      setOpen(true)\n    }\n  })\n\n  return (\n    <motion.div\n      data-slot=\"sticky-banner\"\n      className={cn(\n        \"sticky inset-x-0 top-0 z-40 flex min-h-14 w-full items-center justify-center bg-transparent px-4 py-1\",\n        className,\n      )}\n      initial={{\n        y: -100,\n        opacity: 0,\n      }}\n      animate={{\n        y: open ? 0 : -100,\n        opacity: open ? 1 : 0,\n      }}\n      transition={{\n        duration: 0.3,\n        ease: \"easeInOut\",\n      }}\n      {...props}\n    >\n      {children}\n\n      <motion.button\n        initial={{\n          scale: 0,\n        }}\n        animate={{\n          scale: 1,\n        }}\n        className=\"absolute top-1/2 right-2 -translate-y-1/2 cursor-pointer\"\n        onClick={() => setOpen(!open)}\n        aria-label=\"Fechar banner\"\n      >\n        <CloseIcon className=\"h-5 w-5 text-white\" />\n      </motion.button>\n    </motion.div>\n  )\n}\n\nconst CloseIcon = (props: React.SVGProps<SVGSVGElement>) => {\n  return (\n    <svg\n      xmlns=\"http://www.w3.org/2000/svg\"\n      width=\"24\"\n      height=\"24\"\n      viewBox=\"0 0 24 24\"\n      fill=\"none\"\n      stroke=\"currentColor\"\n      strokeWidth=\"2\"\n      strokeLinecap=\"round\"\n      strokeLinejoin=\"round\"\n      {...props}\n    >\n      <path stroke=\"none\" d=\"M0 0h24v24H0z\" fill=\"none\" />\n      <path d=\"M18 6l-12 12\" />\n      <path d=\"M6 6l12 12\" />\n    </svg>\n  )\n}\n\nexport { StickyBanner }\n",
      "type": "registry:ui"
    },
    {
      "path": "src/components/ui/sticky-banner-types.ts",
      "content": "import type { ReactNode } from \"react\"\n\nexport type StickyBannerProps = {\n  className?: string\n  children: ReactNode\n  hideOnScroll?: boolean\n}\n",
      "type": "registry:ui"
    }
  ],
  "type": "registry:ui"
}